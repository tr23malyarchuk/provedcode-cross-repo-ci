name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  call-frontend-workflow:
    uses: tr23malyarchuk/provedcode-frontend-tr23malyarchuk/.github/workflows/main.yml@main
    with:
      example_input: 'random value pasted'
    secrets:
      PAT: ${{ secrets.PAT }}
